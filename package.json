{
  "name": "signalr-listener",
  "version": "1.0.0",
  "description": "Node.js service to listen to SignalR messages by topic ID and print to console.",
  "main": "index.js",
  "scripts": {
    "start": "npx vite --host=localhost --port=5500 --open"
  },
  "dependencies": {
    "@microsoft/signalr": "^7.0.12",
    "axios": "^1.10.0",
    "vite": "^6.3.5"
  }
}
